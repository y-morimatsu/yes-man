# PWA Icons

Production deploy 前に以下を配置:
- `icon-192.png` (192x192px、PWA manifest 用)
- `icon-512.png` (512x512px、PWA manifest 用)

MVP 段階では Designer 提供前のため placeholder。
生成例: `favicon.svg` を 192px / 512px raster export (Inkscape / Figma)。
