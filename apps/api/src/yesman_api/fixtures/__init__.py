"""Hard-coded fixture data used by mock backend at startup (NFR-1).

Fixtures keep demo / e2e deterministic and provide content for the
anonymous-strangers pool (5 languages × hand-written quotes) when the
real user count is 1.
"""
