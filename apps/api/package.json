{
  "name": "@yesman/api",
  "private": true,
  "version": "0.1.0",
  "description": "YesMan FastAPI backend - venv-managed Python, this package.json exists only to expose lifecycle scripts to the pnpm workspace (e.g. Playwright webServer).",
  "scripts": {
    "start": ".venv/bin/uvicorn yesman_api.main:app --app-dir src --host 0.0.0.0 --port 8000",
    "start:dev": ".venv/bin/uvicorn yesman_api.main:app --app-dir src --host 0.0.0.0 --port 8000 --reload"
  }
}
