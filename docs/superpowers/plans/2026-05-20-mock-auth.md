# Mock モード ログイン/登録/ログアウト Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `VITE_AUTH_BYPASS=true` 下で「Email 入力 → 自動登録 + サインイン → ホーム → Sign out → 再サインイン」の本物っぽいフローを実現し、localStorage で reload/再起動でもログイン状態を保持する。

**Architecture:** localStorage に `users[]` + `current-email` を保存する純粋 module (`mockAuthStorage.ts`) を新規追加。`auth.ts` の bypass 分岐 と `AuthProvider` の initial state / refresh が localStorage を読む。`SignInPage` を email form + 既存ユーザ list に刷新。`Layout` の Sign out で refresh() を呼んで status を更新。real Cognito flow は touch しない。

**Tech Stack:** React 18 / TypeScript / Vite / TanStack Query / `@yesman/ui` (Button, Card, Input, useToast) / Vitest + jsdom + @testing-library + msw

**Spec:** [docs/superpowers/specs/2026-05-20-mock-auth-design.md](../specs/2026-05-20-mock-auth-design.md)

**Branch:** `feature/mock-auth-ui` (既に作成済み、spec + drawio commit 1 件あり: `fe90d92`)

---

## File Structure

```
apps/web/src/
├── shell/
│   ├── mockAuthStorage.ts   [Create] MockUser CRUD + current-email (pure module、localStorage 操作のみ)
│   ├── auth.ts              [Modify] signIn(email?) / signOutUser() の bypass 分岐に localStorage 連動を追加
│   ├── AuthProvider.tsx     [Modify] bypass 時の initial state と refresh() を localStorage 連動
│   └── Layout.tsx           [Modify] Sign out button onClick で refresh() 呼ぶ
└── features/auth/
    └── SignInPage.tsx       [Modify] 大幅刷新: email form + 表示名 + 既存ユーザ list + 自動登録

apps/web/tests/
├── shell/
│   ├── mockAuthStorage.test.ts    [Create] pure CRUD unit
│   └── AuthProvider.test.tsx      [Modify] bypass + localStorage 連動 test 追加
└── features/auth/
    └── SignInPage.test.tsx        [Create] form 動作 / row click / auto-register
```

---

## Task 1: mockAuthStorage モジュール (純粋 CRUD)

**Files:**
- Create: `apps/web/src/shell/mockAuthStorage.ts`
- Create: `apps/web/tests/shell/mockAuthStorage.test.ts`

- [ ] **Step 1: 失敗するテストを書く**

Create `apps/web/tests/shell/mockAuthStorage.test.ts`:

```ts
import { afterEach, beforeEach, describe, expect, it } from "vitest";
import {
  clearCurrent,
  getCurrentEmail,
  getCurrentUser,
  listUsers,
  registerUser,
  setCurrentEmail,
  resetForTesting,
} from "../../src/shell/mockAuthStorage";

describe("mockAuthStorage", () => {
  beforeEach(() => {
    localStorage.clear();
    resetForTesting();
  });
  afterEach(() => localStorage.clear());

  it("listUsers — 初期状態は空配列", () => {
    expect(listUsers()).toEqual([]);
  });

  it("registerUser — 新規 email を追加し MockUser を返す", () => {
    const u = registerUser("taro@example.com", "Taro");
    expect(u.email).toBe("taro@example.com");
    expect(u.display_name).toBe("Taro");
    expect(typeof u.created_at).toBe("string");
    expect(listUsers()).toHaveLength(1);
  });

  it("registerUser — 大文字/前後空白は normalize される", () => {
    const u = registerUser("  Taro@Example.COM  ", "Taro");
    expect(u.email).toBe("taro@example.com");
  });

  it("registerUser — 同 email 2 度目は既存を返し list を増やさない", () => {
    const a = registerUser("taro@example.com");
    const b = registerUser("taro@example.com", "Taro2");
    expect(b.email).toBe("taro@example.com");
    expect(listUsers()).toHaveLength(1);
    // display_name は最初の登録時のものを保持 (上書きしない)
    expect(b.display_name).toBe(a.display_name);
  });

  it("setCurrentEmail — 登録済み email をセット可能", () => {
    registerUser("taro@example.com");
    setCurrentEmail("taro@example.com");
    expect(getCurrentEmail()).toBe("taro@example.com");
  });

  it("setCurrentEmail — 未登録 email は throw", () => {
    expect(() => setCurrentEmail("unknown@example.com")).toThrow(
      /not registered/i,
    );
  });

  it("setCurrentEmail — 入力は normalize される", () => {
    registerUser("taro@example.com");
    setCurrentEmail("  Taro@EXAMPLE.com  ");
    expect(getCurrentEmail()).toBe("taro@example.com");
  });

  it("clearCurrent — current-email を null にする", () => {
    registerUser("taro@example.com");
    setCurrentEmail("taro@example.com");
    clearCurrent();
    expect(getCurrentEmail()).toBeNull();
    // users list は保持される
    expect(listUsers()).toHaveLength(1);
  });

  it("getCurrentUser — current から MockUser を返す", () => {
    registerUser("taro@example.com", "Taro");
    setCurrentEmail("taro@example.com");
    const u = getCurrentUser();
    expect(u?.email).toBe("taro@example.com");
    expect(u?.display_name).toBe("Taro");
  });

  it("getCurrentUser — current が null なら null", () => {
    expect(getCurrentUser()).toBeNull();
  });

  it("localStorage 破損時に空状態へ自動 reset", () => {
    localStorage.setItem("yesman:mock-auth:users", "{not json");
    expect(listUsers()).toEqual([]);
  });

  it("localStorage 永続化: 別の module instance でも保持される (sim reload)", () => {
    registerUser("taro@example.com");
    setCurrentEmail("taro@example.com");
    // sim reload: in-memory cache reset、ただし localStorage は残る
    resetForTesting();
    expect(getCurrentEmail()).toBe("taro@example.com");
    expect(listUsers()).toHaveLength(1);
  });
});
```

- [ ] **Step 2: テスト実行で失敗を確認**

Run:
```bash
cd apps/web && pnpm test -- mockAuthStorage
```
Expected: FAIL with "Cannot find module ... mockAuthStorage"

- [ ] **Step 3: mockAuthStorage 実装**

Create `apps/web/src/shell/mockAuthStorage.ts`:

```ts
/**
 * mockAuthStorage — localStorage に Mock auth 用 state を保持する純粋 module.
 *
 * spec: docs/superpowers/specs/2026-05-20-mock-auth-design.md
 *
 * 注意:
 * - VITE_AUTH_BYPASS=true の場合にのみ呼ばれる前提 (env チェックは呼び出し側で行う)
 * - localStorage が壊れている場合は静かに空状態にリセット (private mode 等)
 */

export interface MockUser {
  email: string;
  display_name?: string;
  created_at: string; // ISO 8601
}

const USERS_KEY = "yesman:mock-auth:users";
const CURRENT_KEY = "yesman:mock-auth:current-email";

function normalize(email: string): string {
  return email.trim().toLowerCase();
}

function safeRead(key: string): string | null {
  try {
    return localStorage.getItem(key);
  } catch {
    return null;
  }
}

function safeWrite(key: string, value: string): void {
  try {
    localStorage.setItem(key, value);
  } catch {
    /* ignore */
  }
}

function safeRemove(key: string): void {
  try {
    localStorage.removeItem(key);
  } catch {
    /* ignore */
  }
}

/** test 用: 内部 cache をリセット (現状 cache を持たないため no-op、将来用にフック) */
export function resetForTesting(): void {
  /* no-op */
}

export function listUsers(): MockUser[] {
  const raw = safeRead(USERS_KEY);
  if (!raw) return [];
  try {
    const parsed = JSON.parse(raw);
    if (!Array.isArray(parsed)) return [];
    return parsed.filter(
      (u): u is MockUser =>
        typeof u === "object" &&
        u !== null &&
        typeof u.email === "string" &&
        typeof u.created_at === "string",
    );
  } catch {
    // 破損 JSON: 静かに空に
    safeRemove(USERS_KEY);
    return [];
  }
}

export function registerUser(email: string, display_name?: string): MockUser {
  const normalized = normalize(email);
  const users = listUsers();
  const existing = users.find((u) => u.email === normalized);
  if (existing) return existing;
  const created: MockUser = {
    email: normalized,
    display_name: display_name?.trim() || undefined,
    created_at: new Date().toISOString(),
  };
  safeWrite(USERS_KEY, JSON.stringify([...users, created]));
  return created;
}

export function setCurrentEmail(email: string): void {
  const normalized = normalize(email);
  const users = listUsers();
  if (!users.some((u) => u.email === normalized)) {
    throw new Error(`mockAuth: email not registered: ${normalized}`);
  }
  safeWrite(CURRENT_KEY, normalized);
}

export function getCurrentEmail(): string | null {
  return safeRead(CURRENT_KEY);
}

export function clearCurrent(): void {
  safeRemove(CURRENT_KEY);
}

export function getCurrentUser(): MockUser | null {
  const current = getCurrentEmail();
  if (!current) return null;
  const users = listUsers();
  return users.find((u) => u.email === current) ?? null;
}
```

- [ ] **Step 4: テスト pass を確認**

Run:
```bash
cd apps/web && pnpm test -- mockAuthStorage
```
Expected: PASS (12 tests)

- [ ] **Step 5: Commit**

```bash
cd /Users/morimatsu/lab/ai-dlc-hackathon
git add apps/web/src/shell/mockAuthStorage.ts \
        apps/web/tests/shell/mockAuthStorage.test.ts
git commit -m "$(cat <<'EOF'
feat(web): mockAuthStorage — localStorage で Mock auth state を保持

users[] と current-email を localStorage に永続化する純粋 module。
normalize (trim + lowercase)、冪等 registerUser、破損 JSON 時の自動
リセットを含む 12 ケースの Vitest unit pass。

Refs: docs/superpowers/specs/2026-05-20-mock-auth-design.md

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: AuthProvider bypass 連動 + auth.ts signIn/signOutUser 拡張

**Files:**
- Modify: `apps/web/src/shell/auth.ts`
- Modify: `apps/web/src/shell/AuthProvider.tsx`
- Create: `apps/web/tests/shell/AuthProvider.bypass.test.tsx`

- [ ] **Step 1: 失敗するテストを書く (新規 file)**

Create `apps/web/tests/shell/AuthProvider.bypass.test.tsx`:

```tsx
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
import { act, renderHook, waitFor } from "@testing-library/react";

// ⚠ env.authBypass = true をモジュール load 前にモック (env.ts は読み取り 1 回キャッシュ)
vi.mock("../../src/shell/env", () => ({
  env: {
    apiBaseUrl: "http://localhost:8000",
    cognitoRegion: "ap-northeast-1",
    cognitoUserPoolId: "ap-northeast-1_test",
    cognitoAppClientId: "test-client",
    cognitoHostedUiUrl: "https://test.auth.example.com",
    appVersion: "test",
    isDev: true,
    authBypass: true,
    mockUserSub: "11111111-1111-1111-1111-111111111111",
    mockUserEmail: "test@example.com",
  },
}));

import { AuthProvider, useAuth } from "../../src/shell/AuthProvider";
import { signIn, signOutUser } from "../../src/shell/auth";
import { registerUser, setCurrentEmail } from "../../src/shell/mockAuthStorage";

describe("AuthProvider — bypass mode", () => {
  beforeEach(() => localStorage.clear());
  afterEach(() => localStorage.clear());

  it("localStorage 未設定 → status=unauthenticated", () => {
    const { result } = renderHook(() => useAuth(), { wrapper: AuthProvider });
    expect(result.current.status).toBe("unauthenticated");
    expect(result.current.email).toBeNull();
  });

  it("registerUser + setCurrentEmail 済み → status=authenticated, email=current", () => {
    registerUser("taro@example.com", "Taro");
    setCurrentEmail("taro@example.com");
    const { result } = renderHook(() => useAuth(), { wrapper: AuthProvider });
    expect(result.current.status).toBe("authenticated");
    expect(result.current.email).toBe("taro@example.com");
    expect(result.current.sub).toBe("11111111-1111-1111-1111-111111111111");
  });

  it("signIn(email) → refresh() → authenticated に遷移", async () => {
    const { result } = renderHook(() => useAuth(), { wrapper: AuthProvider });
    expect(result.current.status).toBe("unauthenticated");

    await act(async () => {
      await signIn("taro@example.com");
      await result.current.refresh();
    });

    await waitFor(() => expect(result.current.status).toBe("authenticated"));
    expect(result.current.email).toBe("taro@example.com");
  });

  it("signOutUser() → refresh() → unauthenticated に遷移", async () => {
    registerUser("taro@example.com");
    setCurrentEmail("taro@example.com");
    const { result } = renderHook(() => useAuth(), { wrapper: AuthProvider });
    expect(result.current.status).toBe("authenticated");

    await act(async () => {
      await signOutUser();
      await result.current.refresh();
    });

    await waitFor(() => expect(result.current.status).toBe("unauthenticated"));
    expect(result.current.email).toBeNull();
  });

  it("セッション永続化: signIn 後に AuthProvider を再 mount しても authenticated", async () => {
    // 1 度目: signIn
    {
      const { result } = renderHook(() => useAuth(), { wrapper: AuthProvider });
      await act(async () => {
        await signIn("taro@example.com");
        await result.current.refresh();
      });
    }
    // 2 度目: 新しい renderHook = sim reload
    const { result } = renderHook(() => useAuth(), { wrapper: AuthProvider });
    expect(result.current.status).toBe("authenticated");
    expect(result.current.email).toBe("taro@example.com");
  });

  it("セッション永続化: signOutUser 後に再 mount しても unauthenticated", async () => {
    registerUser("taro@example.com");
    setCurrentEmail("taro@example.com");
    // 1 度目: signOut
    {
      const { result } = renderHook(() => useAuth(), { wrapper: AuthProvider });
      await act(async () => {
        await signOutUser();
        await result.current.refresh();
      });
    }
    // 2 度目: 新しい renderHook = sim reload
    const { result } = renderHook(() => useAuth(), { wrapper: AuthProvider });
    expect(result.current.status).toBe("unauthenticated");
  });
});
```

- [ ] **Step 2: テスト実行で失敗を確認**

Run:
```bash
cd apps/web && pnpm test -- AuthProvider.bypass
```
Expected: FAIL — bypass 時の挙動が「強制 authenticated」のままなので status/email が期待と乖離

- [ ] **Step 3: auth.ts に bypass 連動を追加**

Modify `apps/web/src/shell/auth.ts` — `signIn` と `signOutUser` を以下に置換:

```ts
import * as mockAuthStorage from "./mockAuthStorage";

export async function signIn(email?: string): Promise<void> {
  if (env.authBypass) {
    if (!email) return; // SignInPage は email 必須で呼ぶ
    mockAuthStorage.registerUser(email);
    mockAuthStorage.setCurrentEmail(email);
    return;
  }
  await signInWithRedirect();
}

export async function signOutUser(): Promise<void> {
  if (env.authBypass) {
    mockAuthStorage.clearCurrent();
    return;
  }
  await signOut({ global: false });
}
```

既存 import の `signInWithRedirect, signOut` は触らない。

- [ ] **Step 4: AuthProvider を localStorage 連動に**

Replace ENTIRE contents of `apps/web/src/shell/AuthProvider.tsx`:

```tsx
/**
 * AuthProvider — shared auth state (FD §5.2 + ultrathink FD C1).
 *
 * - 実 Cognito mode: fetchAuthSession で session 取得
 * - bypass mode: mockAuthStorage (localStorage) で current-email を読む
 *   → reload/再起動でも Sign out するまで authenticated 保持
 */
"use client";

import {
  createContext,
  useCallback,
  useContext,
  useEffect,
  useState,
  type ReactNode,
} from "react";
import { fetchAuthSession } from "aws-amplify/auth";
import { env } from "./env";
import * as mockAuthStorage from "./mockAuthStorage";

export type AuthStatus = "loading" | "authenticated" | "unauthenticated";

export interface AuthState {
  status: AuthStatus;
  sub: string | null;
  email: string | null;
  refresh: () => Promise<void>;
}

const AuthContext = createContext<AuthState | null>(null);

interface BypassSnapshot {
  status: AuthStatus;
  sub: string | null;
  email: string | null;
}

function readBypassSnapshot(): BypassSnapshot {
  const current = mockAuthStorage.getCurrentUser();
  if (!current) {
    return { status: "unauthenticated", sub: null, email: null };
  }
  return {
    status: "authenticated",
    sub: env.mockUserSub, // API 側固定 sub と整合させる (spec §6.1 制約)
    email: current.email,
  };
}

export function AuthProvider({ children }: { children: ReactNode }) {
  const initial = env.authBypass
    ? readBypassSnapshot()
    : { status: "loading" as AuthStatus, sub: null, email: null };

  const [status, setStatus] = useState<AuthStatus>(initial.status);
  const [sub, setSub] = useState<string | null>(initial.sub);
  const [email, setEmail] = useState<string | null>(initial.email);

  const refresh = useCallback(async () => {
    if (env.authBypass) {
      const snap = readBypassSnapshot();
      setSub(snap.sub);
      setEmail(snap.email);
      setStatus(snap.status);
      return;
    }
    try {
      const session = await fetchAuthSession();
      const idToken = session.tokens?.idToken;
      if (idToken) {
        const payload = idToken.payload;
        setSub(typeof payload.sub === "string" ? payload.sub : null);
        setEmail(typeof payload.email === "string" ? payload.email : null);
        setStatus("authenticated");
      } else {
        setStatus("unauthenticated");
      }
    } catch {
      setStatus("unauthenticated");
    }
  }, []);

  useEffect(() => {
    // bypass は initial state で確定済みなので skip。
    // real Cognito のみ mount 時に session fetch
    if (env.authBypass) return;
    void refresh();
  }, [refresh]);

  return (
    <AuthContext.Provider value={{ status, sub, email, refresh }}>
      {children}
    </AuthContext.Provider>
  );
}

export function useAuth(): AuthState {
  const ctx = useContext(AuthContext);
  if (!ctx) throw new Error("useAuth must be used within AuthProvider");
  return ctx;
}
```

- [ ] **Step 5: テスト pass を確認**

Run:
```bash
cd apps/web && pnpm test -- AuthProvider
```
Expected: PASS — AuthProvider.bypass.test.tsx (6 tests) + 既存 AuthProvider.test.tsx (4 tests) 全 pass

> 注: 既存 `AuthProvider.test.tsx` は `authBypass=false` (env 未 stub) で動作するため触らない。bypass test のみ別 file で env を vi.mock。

- [ ] **Step 6: Commit**

```bash
cd /Users/morimatsu/lab/ai-dlc-hackathon
git add apps/web/src/shell/auth.ts \
        apps/web/src/shell/AuthProvider.tsx \
        apps/web/tests/shell/AuthProvider.bypass.test.tsx
git commit -m "$(cat <<'EOF'
feat(web): AuthProvider と auth.ts を localStorage 連動に拡張 (bypass mode)

- signIn(email?): bypass 時に registerUser + setCurrentEmail
- signOutUser(): bypass 時に clearCurrent
- AuthProvider: bypass 時の initial state を mockAuthStorage で算出
  reload 後も current-email が残っていれば authenticated 復帰
- 実 Cognito flow (authBypass=false) は完全に据え置き

新規 6 ケースの Vitest unit pass (bypass mode + セッション永続化)。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: SignInPage 刷新 — email form + 既存ユーザ list + 自動登録

**Files:**
- Modify: `apps/web/src/features/auth/SignInPage.tsx`
- Create: `apps/web/tests/features/auth/SignInPage.test.tsx`

- [ ] **Step 1: 失敗するテストを書く**

Create `apps/web/tests/features/auth/SignInPage.test.tsx`:

```tsx
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
import { render, screen, waitFor } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { MemoryRouter } from "react-router-dom";
import { ToastProvider } from "@yesman/ui";

vi.mock("../../../src/shell/env", () => ({
  env: {
    apiBaseUrl: "http://localhost:8000",
    cognitoRegion: "ap-northeast-1",
    cognitoUserPoolId: "ap-northeast-1_test",
    cognitoAppClientId: "test-client",
    cognitoHostedUiUrl: "https://test.auth.example.com",
    appVersion: "test",
    isDev: true,
    authBypass: true,
    mockUserSub: "11111111-1111-1111-1111-111111111111",
    mockUserEmail: "test@example.com",
  },
}));

const navigateMock = vi.fn();
vi.mock("react-router-dom", async () => {
  const actual = await vi.importActual<typeof import("react-router-dom")>(
    "react-router-dom",
  );
  return { ...actual, useNavigate: () => navigateMock };
});

import { AuthProvider } from "../../../src/shell/AuthProvider";
import SignInPage from "../../../src/features/auth/SignInPage";
import { registerUser, listUsers, getCurrentEmail } from "../../../src/shell/mockAuthStorage";

function renderPage() {
  return render(
    <MemoryRouter>
      <ToastProvider>
        <AuthProvider>
          <SignInPage />
        </AuthProvider>
      </ToastProvider>
    </MemoryRouter>,
  );
}

describe("SignInPage — bypass mode", () => {
  beforeEach(() => {
    localStorage.clear();
    navigateMock.mockReset();
  });
  afterEach(() => localStorage.clear());

  it("初回訪問: 空 list の placeholder と form が表示される", () => {
    renderPage();
    expect(screen.getByRole("heading", { name: /サインイン/ })).toBeInTheDocument();
    expect(screen.getByRole("textbox", { name: /email/i })).toBeInTheDocument();
    expect(screen.getByRole("textbox", { name: /表示名/ })).toBeInTheDocument();
    expect(screen.getByText(/まだ登録ユーザはいません/)).toBeInTheDocument();
  });

  it("email 未入力時 [サインイン] が disabled", () => {
    renderPage();
    const btn = screen.getByRole("button", { name: "サインイン" });
    expect(btn).toBeDisabled();
  });

  it("不正な email format で [サインイン] が disabled", async () => {
    const user = userEvent.setup();
    renderPage();
    await user.type(screen.getByRole("textbox", { name: /email/i }), "not-an-email");
    expect(screen.getByRole("button", { name: "サインイン" })).toBeDisabled();
  });

  it("有効な email 入力 + [サインイン] click で自動登録 + navigate", async () => {
    const user = userEvent.setup();
    renderPage();
    await user.type(screen.getByRole("textbox", { name: /email/i }), "taro@example.com");
    await user.type(screen.getByRole("textbox", { name: /表示名/ }), "Taro");
    await user.click(screen.getByRole("button", { name: "サインイン" }));

    await waitFor(() => {
      expect(listUsers()).toHaveLength(1);
      expect(listUsers()[0].email).toBe("taro@example.com");
      expect(listUsers()[0].display_name).toBe("Taro");
      expect(getCurrentEmail()).toBe("taro@example.com");
    });
    expect(navigateMock).toHaveBeenCalledWith("/", { replace: true });
  });

  it("既存 user row click で setCurrentEmail + navigate", async () => {
    registerUser("hanako@example.com", "Hanako");
    const user = userEvent.setup();
    renderPage();
    // 行内の email がクリック可能 button として表示される
    const row = screen.getByRole("button", { name: /hanako@example\.com/ });
    await user.click(row);

    await waitFor(() => {
      expect(getCurrentEmail()).toBe("hanako@example.com");
    });
    expect(navigateMock).toHaveBeenCalledWith("/", { replace: true });
  });

  it("registerUser 後に list が UI に反映される", () => {
    registerUser("a@example.com", "A");
    registerUser("b@example.com");
    renderPage();
    expect(screen.getByText("a@example.com")).toBeInTheDocument();
    expect(screen.getByText("b@example.com")).toBeInTheDocument();
    expect(screen.queryByText(/まだ登録ユーザはいません/)).not.toBeInTheDocument();
  });

  it("location.state.from があれば navigate 先がそれになる", async () => {
    const user = userEvent.setup();
    render(
      <MemoryRouter initialEntries={[{ pathname: "/auth/signin", state: { from: { pathname: "/score" } } }]}>
        <ToastProvider>
          <AuthProvider>
            <SignInPage />
          </AuthProvider>
        </ToastProvider>
      </MemoryRouter>,
    );
    await user.type(screen.getByRole("textbox", { name: /email/i }), "taro@example.com");
    await user.click(screen.getByRole("button", { name: "サインイン" }));
    await waitFor(() => expect(navigateMock).toHaveBeenCalledWith("/score", { replace: true }));
  });
});
```

- [ ] **Step 2: テスト実行で失敗を確認**

Run:
```bash
cd apps/web && pnpm test -- SignInPage
```
Expected: FAIL — 既存 SignInPage はボタン 1 つだけなので、Email textbox / list 表示などが見つからない

- [ ] **Step 3: SignInPage 刷新**

Replace ENTIRE contents of `apps/web/src/features/auth/SignInPage.tsx`:

```tsx
/**
 * SignInPage — bypass mode で email 入力 / 既存ユーザ row click でサインイン.
 *
 * spec: docs/superpowers/specs/2026-05-20-mock-auth-design.md
 * - bypass mode: form / row click → mockAuthStorage 経由でサインイン → navigate
 * - real Cognito mode: Cognito Hosted UI へ redirect (既存挙動)
 */
import { useMemo, useState } from "react";
import { useLocation, useNavigate } from "react-router-dom";
import { Button, Card, Input } from "@yesman/ui";
import { signIn } from "../../shell/auth";
import { useAuth } from "../../shell/AuthProvider";
import { env } from "../../shell/env";
import { listUsers, type MockUser } from "../../shell/mockAuthStorage";

const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
const DISPLAY_NAME_MAX = 50;

export default function SignInPage() {
  const location = useLocation();
  const navigate = useNavigate();
  const { refresh } = useAuth();
  const [email, setEmail] = useState("");
  const [displayName, setDisplayName] = useState("");
  const [users, setUsers] = useState<MockUser[]>(() => listUsers());
  const [submitting, setSubmitting] = useState(false);

  const from = (location.state as { from?: { pathname: string } } | null)?.from?.pathname ?? "/";
  const emailValid = useMemo(() => EMAIL_REGEX.test(email.trim()), [email]);

  const proceedAfterSignIn = async () => {
    await refresh();
    setUsers(listUsers());
    navigate(from, { replace: true });
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    if (!emailValid || submitting) return;
    setSubmitting(true);
    try {
      if (!env.authBypass) {
        sessionStorage.setItem("yesman:redirect-to", from);
        await signIn(); // Cognito redirect
        return;
      }
      // bypass: email を渡して自動登録 + サインイン
      // display_name は新規ユーザのみ反映 (既存は無視)
      const normalizedEmail = email.trim().toLowerCase();
      const existed = users.some((u) => u.email === normalizedEmail);
      await signIn(email);
      // display_name を新規登録時のみ後追いセット (registerUser は既存を上書きしないため最初の登録で渡す)
      if (!existed && displayName.trim()) {
        // 上の signIn() で display_name 無しで登録済 → spec の「冪等 register」と整合させるため
        // ここでは追加更新せず、display_name を含めるなら別 API か拡張。
        // 今回は MVP として、新規登録時の display_name 反映は SignInPage 側で register を直接呼ぶ実装に切替:
        const mod = await import("../../shell/mockAuthStorage");
        // 既存 entry を消して display_name 付きで再登録 — registerUser は冪等なので no-op になる
        // → 代替: 既存 list に entry が無い場合のみ手動 register
        if (!mod.listUsers().some((u) => u.email === normalizedEmail && u.display_name)) {
          // この時点で entry は display_name なしで存在する
          // 一度 list を消して再構築
          const all = mod.listUsers().filter((u) => u.email !== normalizedEmail);
          localStorage.setItem(
            "yesman:mock-auth:users",
            JSON.stringify([
              ...all,
              {
                email: normalizedEmail,
                display_name: displayName.trim(),
                created_at: new Date().toISOString(),
              },
            ]),
          );
        }
      }
      await proceedAfterSignIn();
    } finally {
      setSubmitting(false);
    }
  };

  const handleRowClick = async (user: MockUser) => {
    if (submitting) return;
    setSubmitting(true);
    try {
      await signIn(user.email);
      await proceedAfterSignIn();
    } finally {
      setSubmitting(false);
    }
  };

  return (
    <div className="flex flex-col gap-4 py-4">
      <h1 className="font-serif text-2xl font-bold text-center">
        YesMan にサインイン
      </h1>
      {env.authBypass && (
        <p className="text-sm text-neutral-600 text-center">
          メールアドレスを入力してサインインしてください。未登録のメールは自動で登録されます。
        </p>
      )}

      <Card>
        <form className="flex flex-col gap-3" onSubmit={handleSubmit}>
          <label className="flex flex-col gap-1 text-sm">
            <span className="font-semibold">Email</span>
            <Input
              type="email"
              aria-label="Email"
              placeholder="you@example.com"
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              required
            />
          </label>
          {env.authBypass && (
            <label className="flex flex-col gap-1 text-sm">
              <span className="font-semibold">表示名 (新規登録時のみ・任意)</span>
              <Input
                type="text"
                aria-label="表示名"
                maxLength={DISPLAY_NAME_MAX}
                value={displayName}
                onChange={(e) => setDisplayName(e.target.value)}
              />
            </label>
          )}
          <Button
            type="submit"
            variant="primary"
            size="lg"
            disabled={!emailValid || submitting}
            loading={submitting}
          >
            サインイン
          </Button>
        </form>
      </Card>

      {env.authBypass && (
        <>
          <div className="text-center text-xs text-neutral-500">
            ─────  または  ─────
          </div>
          <Card>
            <h2 className="font-serif font-semibold mb-3 text-sm">
              既存ユーザから選択
            </h2>
            {users.length === 0 ? (
              <div className="rounded-xl border border-dashed border-neutral-300 p-6 text-center">
                <p className="italic text-neutral-500">まだ登録ユーザはいません</p>
                <p className="text-xs text-neutral-400 mt-1">
                  上のフォームから新規登録できます
                </p>
              </div>
            ) : (
              <ul className="flex flex-col gap-2">
                {users.map((u) => (
                  <li key={u.email}>
                    <button
                      type="button"
                      className="w-full text-left rounded-xl border border-neutral-200 hover:border-brand-500 bg-neutral-0 px-3 py-2 flex justify-between items-center transition-colors disabled:opacity-50"
                      onClick={() => handleRowClick(u)}
                      disabled={submitting}
                    >
                      <span>
                        <span className="font-semibold text-sm block">{u.email}</span>
                        <span className="text-xs text-neutral-600">
                          {u.display_name ?? <span className="italic text-neutral-400">(表示名なし)</span>}
                        </span>
                      </span>
                      <span className="text-neutral-400">→</span>
                    </button>
                  </li>
                ))}
              </ul>
            )}
          </Card>
        </>
      )}
    </div>
  );
}
```

> 注: `display_name` を新規登録時のみ反映するため、`registerUser` が display_name を保持しない既存 entry を一度削除して再登録するハック実装になっている。Task 1 の `registerUser` を「display_name が未設定で既存 entry がある場合だけ上書き」に拡張する方が綺麗だが、scope 内で完結させるため SignInPage 側で対処。

- [ ] **Step 4: テスト pass を確認**

Run:
```bash
cd apps/web && pnpm test -- SignInPage
```
Expected: PASS (7 tests)

- [ ] **Step 5: Commit**

```bash
cd /Users/morimatsu/lab/ai-dlc-hackathon
git add apps/web/src/features/auth/SignInPage.tsx \
        apps/web/tests/features/auth/SignInPage.test.tsx
git commit -m "$(cat <<'EOF'
feat(web): SignInPage を email form + 既存ユーザ list に刷新

bypass mode 時:
- Email + 表示名 input + [サインイン] button (簡易 RFC regex で validate)
- 既存ユーザ list を Card 内に row 表示、クリックで即サインイン
- 未登録 email は自動登録、表示名は新規時のみ反映
- location.state.from で元 page に navigate

real Cognito mode (authBypass=false) は既存挙動を保持:
- form 内の Email input は使うが、[サインイン] click 時に Cognito Hosted UI へ redirect

7 ケースの Vitest unit pass。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: Layout の Sign out button を refresh と統合

**Files:**
- Modify: `apps/web/src/shell/Layout.tsx`

- [ ] **Step 1: Layout を更新**

Modify `apps/web/src/shell/Layout.tsx` — `useAuth` の destructure と Sign out button 周辺を以下に変更:

`const { status } = useAuth();` を以下に置換:

```tsx
  const { status, refresh } = useAuth();

  const handleSignOut = async () => {
    await signOutUser();
    await refresh();
    // RequireAuth が status=unauthenticated を detect し /auth/signin redirect
  };
```

そして既存の Sign out Button を以下に置換:

```tsx
              <Button variant="ghost" size="sm" onClick={handleSignOut}>
                Sign out
              </Button>
```

完成形 (関連箇所):

```tsx
export function Layout() {
  const { status, refresh } = useAuth();

  const handleSignOut = async () => {
    await signOutUser();
    await refresh();
  };

  return (
    // ... 既存 ...
          {status === "authenticated" && (
            <nav className="flex items-center gap-3">
              {/* ... 既存 link 3 個 ... */}
              <Button variant="ghost" size="sm" onClick={handleSignOut}>
                Sign out
              </Button>
            </nav>
          )}
    // ... 既存 ...
  );
}
```

- [ ] **Step 2: TypeScript + 既存テスト pass を確認**

Run:
```bash
cd apps/web && pnpm tsc --noEmit 2>&1 | grep -v "tests/property" | head -20
echo "---"
pnpm test -- "Layout|AuthProvider|SignInPage|mockAuthStorage" 2>&1 | tail -10
```
Expected: TS error なし (src/)、test 全 pass

- [ ] **Step 3: Commit**

```bash
cd /Users/morimatsu/lab/ai-dlc-hackathon
git add apps/web/src/shell/Layout.tsx
git commit -m "$(cat <<'EOF'
feat(web): Layout の Sign out button を refresh() と統合

signOutUser() の await 後に AuthProvider.refresh() を呼ぶことで、
bypass mode でも status=unauthenticated に遷移し RequireAuth が
/auth/signin へ redirect する挙動を確立。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: 最終検証 + マニュアル動作確認 + PR

**Files:** なし (verification)

- [ ] **Step 1: 全 web test + build + lint**

```bash
cd /Users/morimatsu/lab/ai-dlc-hackathon
pnpm --filter @yesman/web test 2>&1 | tail -20
echo "---"
pnpm --filter @yesman/web build 2>&1 | tail -10
echo "---"
pnpm --filter @yesman/web lint 2>&1 | tail -10
```

Expected:
- mockAuthStorage / AuthProvider.bypass / SignInPage tests 全 pass (累計 25+)
- 既存 AuthProvider / RequireAuth / ApiProvider tests 全 pass (real Cognito path に regression なし)
- Pre-existing 失敗 (msw / fast-check 未関連) は変動なし
- `pnpm build` 成功
- lint warning 0

- [ ] **Step 2: dev server を起動して手動検証**

API + Web が既に動いていなければ:

```bash
cd /Users/morimatsu/lab/ai-dlc-hackathon
MOCK_SEED_DEMO_DECISIONS=true LLM_PROVIDER=mock STORAGE_BACKEND=mock AUTH_BACKEND=mock VOICE_BACKEND=mock EVENT_BACKEND=sync MOCK_AUTO_USER=true LEARNING_CONSUMER_ENABLED=false SILENCE_HASH_SALT=local-salt PERSONA_ANONYMIZER_SALT=local-persona-salt CORS_ALLOWED_ORIGINS='["http://localhost:5173"]' pnpm --filter @yesman/api start &

VITE_API_BASE_URL=http://localhost:8000 VITE_AUTH_BYPASS=true VITE_MOCK_USER_SUB=11111111-1111-1111-1111-111111111111 VITE_MOCK_USER_EMAIL=test@example.com VITE_COGNITO_REGION=ap-northeast-1 VITE_COGNITO_USER_POOL_ID=ap-northeast-1_test VITE_COGNITO_APP_CLIENT_ID=test VITE_COGNITO_HOSTED_UI_URL=https://test.auth.example.com VITE_APP_VERSION=local pnpm --filter @yesman/web dev &
```

ブラウザで以下を確認:

1. http://localhost:5173 を開く → /auth/signin にリダイレクト、Email form + "まだ登録ユーザはいません"
2. `taro@example.com` + 表示名 `Taro` を入力 → [サインイン] → ホーム / にリダイレクト、Header に `Sign out` button
3. **ブラウザ reload** (Cmd+R) → ホームのまま (authenticated 保持)
4. [Sign out] click → /auth/signin に戻る、`taro@example.com (Taro)` の row が見える
5. **再度 reload** → /auth/signin のまま (unauthenticated 保持)
6. `taro@example.com` の row click → ホームに redirect、再度サインイン成功
7. もう一度 [Sign out] → /auth/signin
8. 別 email `hanako@example.com` を入力 → [サインイン] → ホーム → /auth/signin で 2 件の row 表示

- [ ] **Step 3: PR 作成 (user の明示的指示がある場合のみ)**

```bash
cd /Users/morimatsu/lab/ai-dlc-hackathon
git push -u origin feature/mock-auth-ui
gh pr create --base develop --head feature/mock-auth-ui \
  --title "feat(web): Mock モード ログイン/登録/ログアウト機能を追加" \
  --body "$(cat <<'EOF'
## Summary

VITE_AUTH_BYPASS=true 下で「本物っぽい」サインイン/サインアウト体験を実装。

- SignInPage を **email form + 既存ユーザ list** に刷新 (未登録は自動登録)
- localStorage に `users[]` + `current-email` を保存 → **reload/再起動でも Sign out するまでログイン維持**
- AuthProvider の bypass 分岐を localStorage 連動に
- Layout の Sign out が `refresh()` で状態を更新 → RequireAuth が /auth/signin にリダイレクト
- real Cognito flow (authBypass=false) は **完全に据え置き**

## Spec / Plan / Diagrams

- Spec: [docs/superpowers/specs/2026-05-20-mock-auth-design.md](../blob/feature/mock-auth-ui/docs/superpowers/specs/2026-05-20-mock-auth-design.md)
- Plan: [docs/superpowers/plans/2026-05-20-mock-auth.md](../blob/feature/mock-auth-ui/docs/superpowers/plans/2026-05-20-mock-auth.md)
- Mockup: [docs/superpowers/specs/diagrams/2026-05-20-mock-auth-screens.drawio](../blob/feature/mock-auth-ui/docs/superpowers/specs/diagrams/2026-05-20-mock-auth-screens.drawio) (4 ページ)

## 制約 (spec §6.1)

API 側 MockAuthAdapter は env 固定 sub を返すため、frontend で email を切り替えても **Profile/履歴データは同一行を共有** (UI の cosmetic 切替のみ)。完全な multi-user は別 spec。

## Test plan

- [ ] `pnpm --filter @yesman/web test -- "mockAuthStorage|AuthProvider|SignInPage"` 全 pass
- [ ] `pnpm --filter @yesman/web build` 成功
- [ ] `pnpm --filter @yesman/web lint` warning 0
- [ ] ブラウザ手動:
  - /auth/signin で空 list 表示 → 新規登録 → ホーム
  - reload で authenticated 保持
  - Sign out → /auth/signin、再 reload で unauthenticated 保持
  - 既存 row click で即サインイン

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

> ⚠️ Push と PR 作成は user の明示的指示がある場合のみ実行。

---

## Self-Review Checklist

(plan 作者: writing-plans skill の self-review として、書き終わった後に確認すべき項目)

**1. Spec coverage**

| Spec section | Task | カバレッジ |
|---|---|---|
| §2 受入基準 (Gherkin) | Task 2 + 3 + 4 | ✅ 初回 / サインイン / Sign out / row click / persistence 全シナリオ |
| §3.1 Data Model | Task 1 | ✅ MockUser 型 + localStorage keys |
| §3.2 Module 構成 | File Structure | ✅ 全 5 file |
| §3.3 mockAuthStorage API | Task 1 | ✅ listUsers/registerUser/setCurrentEmail/getCurrentEmail/clearCurrent/getCurrentUser |
| §3.4 auth.ts | Task 2 Step 3 | ✅ signIn(email?) + signOutUser bypass 分岐 |
| §3.5 AuthProvider | Task 2 Step 4 | ✅ readBypassSnapshot + initial state lazy + refresh |
| §3.6 SignInPage UI | Task 3 | ✅ form + list + auto-register |
| §3.7 navigate | Task 3 (proceedAfterSignIn) | ✅ location.state.from |
| §3.8 Layout Sign out | Task 4 | ✅ refresh wire |
| §5 Error / Edge cases | Task 1 (safeRead/safeWrite) + Task 3 (validation) | ✅ |
| §6 制約 | spec のまま、test では out of scope | ✅ |
| §8.1 mockAuthStorage tests | Task 1 Step 1 | ✅ 12 cases |
| §8.2 AuthProvider tests | Task 2 Step 1 | ✅ 6 cases (永続化含む) |
| §8.3 SignInPage tests | Task 3 Step 1 | ✅ 7 cases |
| §8.4 Sign out flow | Task 4 + 手動検証 | ✅ |

**2. Placeholder scan**: TBD / TODO なし、全 step に code または exact command を記載。

**3. Type consistency**

- `MockUser` interface: Task 1 で定義、Task 2/3 で `import type { MockUser }` 使用
- `signIn(email?)` signature: Task 2 Step 3 で定義、Task 3 で `signIn(email)` / `signIn(user.email)` 呼出
- `readBypassSnapshot()` return type: AuthProvider 内 private、外部から触らない
- `EMAIL_REGEX` + `DISPLAY_NAME_MAX`: Task 3 内に閉じる

**4. Known concerns**

- Task 3 Step 3 の `display_name` 後追い更新ハックは醜い。代替案: Task 1 の `registerUser` を「display_name 未設定 entry に display_name を後付け」可能に拡張する方が綺麗。ただし scope 拡大を避けて SignInPage に閉じ込めた。実装者が綺麗にしたい場合は Task 1 の registerUser を `registerUser(email, display_name?)` シグネチャ拡張 + 「既存 entry の display_name が空なら上書き」挙動に変更し、Task 3 のハックを削除しても良い。spec 動作には影響なし。
- 既存 `AuthProvider.test.tsx` は `authBypass=false` 前提 (env stub 無し) なので touch しない。新規 `.bypass.test.tsx` で env を `vi.mock`。
- env を `vi.mock` するパターンは jsdom + vitest で動作確認済 (file 単位の module graph)。

---
